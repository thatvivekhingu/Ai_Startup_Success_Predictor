import React from 'react';
import { Mail, ShieldCheck } from 'lucide-react';
import { Button, EvidenceBadge, PageHeader, StatePanel } from '../components/workspace/UiPrimitives';

const AccessPage = ({ onNavigate }) => <div className="sp-page"><PageHeader eyebrow="Utility / Access" title="Pricing & access" description="Review the access state for this deployment. Billing tiers are not published in the current product configuration." status={<><span>Access information is configuration-backed only.</span><EvidenceBadge>Pricing not published</EvidenceBadge></>} /><div className="sp-grid-two"><section className="sp-panel"><div className="sp-panel-header"><div className="sp-panel-heading"><span className="sp-eyebrow">Current deployment</span><h2 className="sp-panel-title">Workspace access</h2></div><ShieldCheck size={18} className="text-sp-teal" /></div><div className="sp-panel-body grid gap-4"><div className="sp-stat-row"><div className="sp-stat"><span className="sp-stat-label">Evaluate</span><span className="sp-stat-value is-teal text-sm">Available</span></div><div className="sp-stat"><span className="sp-stat-label">Portfolio</span><span className="sp-stat-value is-teal text-sm">Available</span></div><div className="sp-stat"><span className="sp-stat-label">Evidence</span><span className="sp-stat-value is-teal text-sm">Available</span></div><div className="sp-stat"><span className="sp-stat-label">Billing</span><span className="sp-stat-value is-amber text-sm">Unpublished</span></div></div><StatePanel status="warning" title="Pricing information is not published for this deployment."><p>No plan names, prices, trials, discounts, or entitlements are shown until billing configuration is connected.</p></StatePanel><Button variant="primary" onClick={() => onNavigate('help')}><Mail size={14} />Contact through Help & docs</Button></div></section><section className="sp-panel"><div className="sp-panel-header"><div className="sp-panel-heading"><span className="sp-eyebrow">What is available</span><h2 className="sp-panel-title">Included workspace areas</h2></div></div><div className="sp-panel-body"><ul className="grid gap-3 text-sm text-sp-muted">{['Run a venture estimate from model-backed fields', 'Review returned saved evaluations', 'Inspect model evidence and reference cohorts', 'Research funding records and sources', 'Draft and review student workspace material'].map((item) => <li key={item} className="flex items-start gap-2"><span className="mt-1 h-1.5 w-1.5 rounded-full bg-sp-teal" />{item}</li>)}</ul></div></section></div></div>;

export default AccessPage;
